# Test Automation Assignment

Registration number: it23628786

This workspace contains a Python Playwright script that reads test cases from an Excel workbook, runs the UI test flow, and writes the actual output and status back into the sheet.

## Files to submit

- `it23628786_test_automation.py`
- `Assignment 1 - Test cases.xlsx` (to be completed separately)
- `it23628786_README.md`
- `it23628786_requirements.txt`

Do not submit `__pycache__` or `.pyc` files.

## Requirements

- Python 3.11+ recommended
- `playwright`
- `openpyxl`

## How to run

From the workspace root, install dependencies and run:

```bash
pip install -r it23628786_requirements.txt
python test_automation/it23628786_test_automation.py --help
```

To execute the assignment against the workbook (once completed):

```bash
python test_automation/it23628786_test_automation.py
```

By default, the script looks for the assignment workbook in the same folder and saves the results back to the Excel file.

## Notes

- The Excel sheet will be completed later and submitted separately.
- The script passes syntax and startup checks.
